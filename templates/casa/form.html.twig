{% extends 'base.html.twig' %}

{% block title %}Nueva casa{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4 text-success">🏡 Nueva casa</h1>

    <div class="card shadow-sm">
        <div class="card-body">
            {{ form_start(form) }}
                {{ form_row(form.direccion) }}
                {{ form_row(form.precio) }}
                {{ form_row(form.descripcion) }}
                {{ form_row(form.provincia) }}
                <div class="mt-3">
                    {{ form_row(form.guardar) }}
                </div>
            {{ form_end(form) }}
        </div>
    </div>

    <a href="{{ path('lista_casas') }}" class="btn btn-outline-secondary mt-3">⬅️ Volver al listado</a>
</div>
{% endblock %}